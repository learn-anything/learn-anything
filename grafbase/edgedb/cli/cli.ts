import { getAllGlobalLinks } from "../crud/global-link"
import { updateLearningStatus } from "../queries/updateLearningStatus.query"

async function main() {
  // const links = await getAllGlobalLinks()
  // console.log(links, "links")
  // await updateLearningStatus("")
}

await main()
