{
	"name": "learn-anything",
	"scripts": {
		"dev": "bun web",
		"web": "cd web && bun dev",
		"web:build": "bun run --filter '*' build",
		"cli": "bun run --watch cli/run.ts",
		"seed": "bun --watch cli/seed.ts"
	},
	"devDependencies": {
		"bun-types": "^1.1.21"
	},
	"workspaces": [
		"web"
	],
	"prettier": {
		"useTabs": true,
		"semi": false,
		"trailingComma": "none",
		"printWidth": 120,
		"arrowParens": "avoid"
	},
	"private": true,
	"dependencies": {
		"jazz-nodejs": "^0.7.23",
		"react-icons": "^5.2.1"
	}
}
